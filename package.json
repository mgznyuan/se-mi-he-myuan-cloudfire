{
  "name": "se-mi-viewer-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public",
    "deploy": "npx wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.9.1" // Or your installed version
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.28.1", // Ensure this line exists with a version
    "itty-router": "^4.0.27"       // Ensure this line exists with a version
  }
}