{
  "name": "se-mi-viewer-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev .",
    "deploy": "npx wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.9.1"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.29.0",
    "itty-router": "^4.2.2"
  },
  "scripts": {
  "build": "esbuild functions/_worker.js --bundle --outfile=dist/_worker.js --format=esm --platform=browser"
  }
}
