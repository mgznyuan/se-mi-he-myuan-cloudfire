{
  "name": "se-mi-health-equity-viewer",
  "version": "1.0.0",
  "description": "Frontend and Worker backend for the SE MI Health Equity Viewer",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "wrangler pages dev . --r2=DATA_BUCKET",
    "deploy": "wrangler pages deploy .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Meixin Yuan",
  "license": "UNLICENSED",
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.28.0",
    "itty-router": "^4.0.27"
  },
  "devDependencies": {
    "wrangler": "^4.9.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
